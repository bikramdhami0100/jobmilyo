import * as React from 'react';
// import { Html } from '@react-email/html';
import { Button } from '@/components/ui/button';
import Myfun from './EmailTemplate';

export function Email() {
 

  return (
    <div >
        
      <Button  onClick={()=>{
        
      }}>Click me</Button>
    </div>
  );
}

export default Email;
